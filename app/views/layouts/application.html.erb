<!DOCTYPE html>
<html>
  <head>
    <title>Twitter</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>

    <ul class="navbar bg-dark">
      <li class='nav-item'><%= link_to "HOME", timeline_path, class: "navbar-brand mb-0 text-light" %></li>
      <li class='nav-item'><%= link_to "Timeline", timeline_path, class: "nav-link text-light" %></li>
      <li class='nav-item'><%= link_to "Users", users_path, class: "nav-link text-light"%></li>
      <% if current_user %>
        <li class='nav-item'><%= link_to "Edit Profile", edit_user_registration_path(id: current_user.id), class: "nav-link text-light" %></li>
        <li class='nav-item'><%= link_to "Sign Out", destroy_user_session_path,  method: :delete,  class: "nav-link text-light" %></li>

      <% else %>
      <li class='nav-item'><%= link_to "Sign In", new_user_session_path %></li>
      <%  end %>
    </ul>
  <div class="container">
    <% if notice %>
      <p class="notice bg-success text-light"><%= notice %></p>
    <% end %>
    <% if alert %>
      <p class="alert bg-danger text-light"><%= alert %></p>
    <% end %>
    <%= yield %>
  </div>
  </body>
</html>
